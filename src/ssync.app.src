{application, ssync, [
    {description,  "Sync for rebarized projects"},
    {vsn,          git},
    {mod,          {ssync_app, []}},
    {applications, [kernel, stdlib, erlinotify]},
    {registered, []},
    {env, [
        {outputs, ['notify-send', console]},
        {timeout, 2000},
        {extensions, [".erl", ".hrl", ".c", ".cpp", ".cc", ".h", ".hpp", ".hh", ".src"]},
        {dirs, [
            {"", watch_rebar_config},
            {"src", compile},
            {"c_src", compile},
            {"include", compile},
            {"ebin", reload}
        ]}
    ]}
]}.
